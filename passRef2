#include <stdio.h>
#include <string.h>

void square (int const *x);
 

int main(void)
{
    /* local variable definition*/
    int n;
    int *pn = NULL;

    /* Setting pointer to value*/
    pn = &n;

    printf("Please enter a number\n");
    scanf("%d", pn);

    printf("You entered %d. The square root of %d is %d.\n", pn, pn, square(pn));

   
    return 0; 
}

void square (int const *x);
{
    *x = (*x) * (*x); // save the value of x
    

    return square;
}
